# Changelog

## v0.5.62 - 2026-09-05

- 未生成出结果的超时、断网、上游异常和提交中断任务统一标记为“生成失败”，不再进入待核对锁定状态。
- 画布节点和任务中心均可直接重新发起失败任务，不再要求二次确认扣费。
- 升级后自动将旧版“提交待核对”记录转为可重试的失败任务，同时清除画布上的历史锁定标记。
- 保留现有项目、生成结果、账户配置、安装位置和桌面快捷方式。

## v0.5.61 - 2026-09-05

- 修复 0.5.60 Windows 安装包漏装 `electron-updater` 运行依赖、更新后主进程无法启动的问题。
- Windows 打包现在拒绝使用解析到项目目录外的 `node_modules`，避免目录联接或外置依赖被打包器静默漏掉。
- 安装包复制到发布目录前会验证 `electron-updater` 的完整依赖链；任一依赖缺失或解析到安装包外部都会立即终止发布。
- 新增缺失依赖回归测试，并要求使用 E 盘干净工作区执行完整依赖安装、构建和启动验证。
- 保留现有项目、生成结果、提示词模板、设置、安装位置和桌面快捷方式。

## v0.5.60 - 2026-09-05

- 批量工厂新增“AI 智能分析提示词”，可选择已配置的 GPT 提示词模型或 Qwen 视觉分析模型，结合修改需求、产品图和可选对标图生成图生图提示词。
- 智能分析一次读取前 4 张产品图和 1 张对标图，清楚区分图片角色；结果先显示预览，只有点击采用后才替换当前提示词。
- 新增本机提示词库，可命名保存当前提示词、在后续批次中直接选择复用或删除；模板使用原子文件保存，重启后仍保留。
- 视觉分析继续使用本机加密账户凭据，API Key 不进入前端、项目、批次或提示词模板；已配置的图片生成模型不会被误当成视觉对话模型。
- 保留现有项目、批次、生成结果、同步记录、设置、安装位置和桌面快捷方式。

## v0.5.59 - 2026-09-05

- “小红书素材盘”可通过稳定客户端身份自动配对，并出现在批量工厂的“一键同步”软件选择窗口。
- 新增按客户端隔离的同步收件箱与完成确认；小红书发布台只会收到明确分配给自己的成品图片。
- 素材盘连接使用只读最小权限，禁止访问其他软件的清单、未分配媒体以及 Vela 的配置或写入接口。
- 局域网配对增加只读权限强制、失败限速和旧全权限连接隔离；跨网页请求在解析内容前即被拒绝。
- 同步清单支持逐项确认；等待接收的项目不会被提前删除，旧版遗留且已无接收方的清单会安全归档。
- 保留现有 Storyworks 同步方式、项目、任务、云端算力配置和本机数据。

## v0.5.58 - 2026-09-04

- 批量工厂的“一键同步”现在会先打开软件选择窗口，可同时选择 Storyworks 和多个已配对的自制软件。
- 每个目标软件获得独立同步清单，并在窗口中分别显示成功或失败；单个目标失败不影响其他目标。
- 同步窗口会记住上次选择、支持刷新和全选；首次升级仍默认只选 Storyworks，避免意外扩大同步范围。
- 设置页将连接区统一为“外部软件连接”；配对令牌继续单独加密保存，不进入项目、日志或同步清单。

## v0.5.57 - 2026-09-04

- 批量工厂新增一张全批次共享的“对标图”输入；每条工作流在画布中清楚呈现“商品原图 + 对标图 → 换装主图”的双参考结构。
- 新建批次可自动附加“姿势裂变”后缀节点，默认生成 5 张自然且不重复的独立姿势图，只改变姿势并锁定人物、服装、背景、构图、光线和文字等细节。
- 主图未生成完成时会阻止姿势后缀误启动；多张主图可点击选定满意图作为后续工作流输入，刷新或重新打开项目后仍保留选择。
- 对标素材只在项目中持久化一次，旧批次继续按原有结构读取，不改写已有项目或用户数据。

## v0.5.56 - 2026-09-04

- 修复“批量工厂”把多张输入图创建成多个项目的问题；现在一个批次只创建一个项目，每张图在项目画布内形成独立工作流分组。
- 每个工作流继续独立提交、跟踪进度和保存结果，并在 Storyworks 同步清单中携带工作流分组标识。
- 旧版多项目批次会无损合并为一个可见项目；旧项目与历史任务留在本地作为兼容数据，不再重复显示。
- 首页增加“批量工厂”项目分类，普通项目与批量项目分开显示。
- 批量页移除旧说明标题并增加“新建项目”入口。
- 批量任务完成后会把成品图、成功状态和 100% 进度写回画布；打开旧项目时自动修复停留在 0% 的节点。

## v0.5.55 - 2026-09-04

- 新增“批量工厂”：一次上传多张图片，为每张图创建包含原图与 GPT 图生图节点的独立项目。
- 支持共享设置项目名前缀、画面比例、1K/2K/4K、GPT 图片模型、每图张数和图生图提示词。
- 支持勾选项目后批量开始生成，已提交项目不会重复启动，并实时展示每个项目的进度与结果。
- 支持一键发布 Storyworks 批次同步清单；同步过程不覆盖既有 Storyworks 项目或素材。

## v0.5.53 - 2026-08-21

- 设置页提供本机画布连接地址、一次性配对码与连接撤销能力，外部编导工具可安全连接当前 Vela 画布。
- AutoDL 账号、余额及两台 MiniMax H3 GPU 凭据持久化保存，关闭重开后继续保持绑定，并支持按任务自动开机、空闲自动关机。
- Storyworks 导入、项目与短剧分区、GPT 图片中转、MiniMax H3 Ref2VA 多参考图视频生成及动态端口发现均整合在当前桌面版本中。
- 保留当前源码、API 与 AutoDL H3 部署资料；历史安装包、旧工程、旧任务文档、QA 截图和临时缓存已从工作目录移除。
