# v0.5.24 QA

## Automated

- `npx tsc --noEmit`：通过。
- `npm run test:node`：80/80 通过。
- `npm run build`：通过。

## Browser interaction

使用独立临时数据目录创建两图片节点和一个分组：

1. 两个图片节点均显示“替换图片”和“编辑文字标注”按钮。
2. 第一节点原位替换为另一张 PNG 后，媒体 URL 改变；节点 ID `qa-image-a`、坐标 `(260, 220)`、分组 ID `qa-group` 不变。
3. 节点说明保存为两行文字，字号 34px，颜色 `#B42318`。
4. 分组名称保存为“买家秀首图工作流”，字号 30px，颜色 `#126B52`。
5. 保存、刷新并重新打开项目后，上述节点说明、分组名称、字号、颜色和替换后的图片仍存在。
6. 标注显示在图片内部上方，不与分组标题重叠；上传失败恢复原图的代码路径已覆盖。

## Desktop continuity

待 Windows 原位安装完成后记录版本、快捷方式和用户数据连续性。

- `npm run build:win`：通过，生成 `Vela-Setup-0.5.24-x64.exe`。
- 原安装目录保持为 `D:\桌面\vela-ai-canvas`。
- 原桌面快捷方式仍指向 `D:\桌面\vela-ai-canvas\Vela AI视频画布.exe`。
- 安装后从原快捷方式重新打开，已安装版本为 `0.5.24`。
- 升级前后两个项目 ID 完全一致，1 个已配置账号仍在，安装时无正在运行的生成任务。
- 安装前后 `app.asar` 哈希不同，确认新程序文件已覆盖。
- 本次未上传 GitHub。
